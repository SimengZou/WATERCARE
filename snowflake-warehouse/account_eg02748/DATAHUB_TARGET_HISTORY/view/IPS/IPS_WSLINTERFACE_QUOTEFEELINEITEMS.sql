CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLINTERFACE_QUOTEFEELINEITEMS AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        DATALAKE_DELETED, 
        FEEAMOUNT, 
        FEEQTY, 
        FEEVALUE, 
        MODBY, 
        MODDTTM, 
        QUOTEFEELINEITEMSKEY, 
        QUOTEGENERATIONSTAGINGKEY, 
        SERVICENAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLINTERFACE_QUOTEFEELINEITEMS
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        DATALAKE_DELETED, 
        FEEAMOUNT, 
        FEEQTY, 
        FEEVALUE, 
        MODBY, 
        MODDTTM, 
        QUOTEFEELINEITEMSKEY, 
        QUOTEGENERATIONSTAGINGKEY, 
        SERVICENAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLINTERFACE_QUOTEFEELINEITEMS;