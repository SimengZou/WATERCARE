CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLDIRECTDEBIT_DIRECTDEBITEXT AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        DELETED, 
        DIRECTDEBITEXTKEY, 
        DIRECTDEBITRUNKEY, 
        ISPROCESSED, 
        MODBY, 
        MODDTTM, 
        SCHEDULEHISTORYKEY, 
        SCHEDULEKEY, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLDIRECTDEBIT_DIRECTDEBITEXT
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        DELETED, 
        DIRECTDEBITEXTKEY, 
        DIRECTDEBITRUNKEY, 
        ISPROCESSED, 
        MODBY, 
        MODDTTM, 
        SCHEDULEHISTORYKEY, 
        SCHEDULEKEY, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLDIRECTDEBIT_DIRECTDEBITEXT;