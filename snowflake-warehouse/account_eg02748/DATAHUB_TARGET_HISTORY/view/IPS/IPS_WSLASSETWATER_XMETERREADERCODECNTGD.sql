CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLASSETWATER_XMETERREADERCODECNTGD AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COUNT, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        READERCODE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XMETERDETAIL, 
        XMETERREADERCODECNTGDKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLASSETWATER_XMETERREADERCODECNTGD
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COUNT, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        READERCODE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XMETERDETAIL, 
        XMETERREADERCODECNTGDKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLASSETWATER_XMETERREADERCODECNTGD;