CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_PLANCOPYLOG AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APKEY, 
        BYTYPE, 
        CHKDTTM, 
        COMMONPLANCOPYKEY, 
        CONTACTKEY, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        OUTSOURCE, 
        PLANCOPYLOGKEY, 
        PLANLOCATION, 
        REVIEWER, 
        STATUS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VERSION, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_PLANCOPYLOG
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APKEY, 
        BYTYPE, 
        CHKDTTM, 
        COMMONPLANCOPYKEY, 
        CONTACTKEY, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        OUTSOURCE, 
        PLANCOPYLOGKEY, 
        PLANLOCATION, 
        REVIEWER, 
        STATUS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VERSION, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_PLANCOPYLOG;