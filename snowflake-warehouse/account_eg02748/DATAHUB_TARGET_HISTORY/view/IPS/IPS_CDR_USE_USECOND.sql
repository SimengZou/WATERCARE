CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_USE_USECOND AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLDBY, 
        APPLDDTTM, 
        APRV, 
        APRVBY, 
        APRVDTTM, 
        APRVED, 
        APUSECONDKEY, 
        APUSECONDTYPEKEY, 
        APUSEKEY, 
        ASSIGNED, 
        CMPTRGEN, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        SUPREQD, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_USE_USECOND
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLDBY, 
        APPLDDTTM, 
        APRV, 
        APRVBY, 
        APRVDTTM, 
        APRVED, 
        APUSECONDKEY, 
        APUSECONDTYPEKEY, 
        APUSEKEY, 
        ASSIGNED, 
        CMPTRGEN, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        SUPREQD, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_USE_USECOND;