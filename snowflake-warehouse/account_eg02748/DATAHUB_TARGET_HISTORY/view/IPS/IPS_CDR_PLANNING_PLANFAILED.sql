CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_PLANNING_PLANFAILED AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLANFAILKEY, 
        APPLANINSPKEY, 
        CMPLDTTM, 
        COMMENTS, 
        DELETED, 
        FAILED, 
        LOC, 
        MODBY, 
        MODDTTM, 
        STATUS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VIOLDTTM, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_PLANNING_PLANFAILED
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLANFAILKEY, 
        APPLANINSPKEY, 
        CMPLDTTM, 
        COMMENTS, 
        DELETED, 
        FAILED, 
        LOC, 
        MODBY, 
        MODDTTM, 
        STATUS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VIOLDTTM, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_PLANNING_PLANFAILED;