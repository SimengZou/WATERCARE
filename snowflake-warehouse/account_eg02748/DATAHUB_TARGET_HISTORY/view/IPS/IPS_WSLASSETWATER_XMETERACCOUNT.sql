CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLASSETWATER_XMETERACCOUNT AS 
    SELECT 
        "ACCOUNT", 
        ACCTSRVPOSKEY, 
        ADDBY, 
        ADDDTTM, 
        ADDRESS, 
        COMPKEY, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        SERVICETYPE, 
        SHAREPERC, 
        STARTDTTM, 
        SUBTRACTFLAG, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XMETERACCOUNTKEY, 
        XWATERMETERDETAILKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLASSETWATER_XMETERACCOUNT
    UNION ALL 
    SELECT 
        "ACCOUNT", 
        ACCTSRVPOSKEY, 
        ADDBY, 
        ADDDTTM, 
        ADDRESS, 
        COMPKEY, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        SERVICETYPE, 
        SHAREPERC, 
        STARTDTTM, 
        SUBTRACTFLAG, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XMETERACCOUNTKEY, 
        XWATERMETERDETAILKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLASSETWATER_XMETERACCOUNT;