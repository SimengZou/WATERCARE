CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_RESOURCES_ESCROWACCT AS 
    SELECT 
        ACCTSTATUS, 
        ACCTSTATUSBY, 
        ACCTSTATUSDTTM, 
        ACCTTYPE, 
        ADDBY, 
        ADDDTTM, 
        CNTCTKEY, 
        DELETED, 
        DESCRIPTION, 
        ESCROWACCTKEY, 
        ESCROWACCTNO, 
        LASTTRANDTTM, 
        MAXBALVALUE, 
        MINBALVALUE, 
        MODBY, 
        MODDTTM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_RESOURCES_ESCROWACCT
    UNION ALL 
    SELECT 
        ACCTSTATUS, 
        ACCTSTATUSBY, 
        ACCTSTATUSDTTM, 
        ACCTTYPE, 
        ADDBY, 
        ADDDTTM, 
        CNTCTKEY, 
        DELETED, 
        DESCRIPTION, 
        ESCROWACCTKEY, 
        ESCROWACCTNO, 
        LASTTRANDTTM, 
        MAXBALVALUE, 
        MINBALVALUE, 
        MODBY, 
        MODDTTM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_RESOURCES_ESCROWACCT;