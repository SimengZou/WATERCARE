CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_RESOURCES_IDENTITYALERT AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ALERTDEFINITIONKEY, 
        ALERTTYPE, 
        DATALAKE_DELETED, 
        DELINQUENCYMSALERTKEY, 
        EFFDATE, 
        EXPDATE, 
        IDENTITYALERTKEY, 
        IDENTITYKEY, 
        INITIATEDBY, 
        MODBY, 
        MODDTTM, 
        REASON, 
        RESOLUTION, 
        RESOLVEDBY, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_RESOURCES_IDENTITYALERT
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ALERTDEFINITIONKEY, 
        ALERTTYPE, 
        DATALAKE_DELETED, 
        DELINQUENCYMSALERTKEY, 
        EFFDATE, 
        EXPDATE, 
        IDENTITYALERTKEY, 
        IDENTITYKEY, 
        INITIATEDBY, 
        MODBY, 
        MODDTTM, 
        REASON, 
        RESOLUTION, 
        RESOLVEDBY, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_RESOURCES_IDENTITYALERT;