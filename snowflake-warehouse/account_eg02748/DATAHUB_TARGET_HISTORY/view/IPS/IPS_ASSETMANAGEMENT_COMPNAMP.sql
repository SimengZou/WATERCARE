CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_ASSETMANAGEMENT_COMPNAMP AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMPKEY, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        NPCODE, 
        NPVALUE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_ASSETMANAGEMENT_COMPNAMP
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMPKEY, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        NPCODE, 
        NPVALUE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_ASSETMANAGEMENT_COMPNAMP;