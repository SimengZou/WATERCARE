CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_BILLING_APPROVALLEVEL AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPROPTIONSPECIFIC, 
        APPROVALLEVELKEY, 
        APPROVALLEVELNAME, 
        APPROVALOPTION, 
        APPROVALOPTIONFORMULA, 
        APPROVALSCHEME, 
        APPROVEUPTOAMOUNT, 
        BYPASSAMOUNT, 
        COSIGN, 
        DELETED, 
        DESCRIPTION, 
        MODBY, 
        MODDTTM, 
        REJECTIONOPTION, 
        REJOPTIONSPECIFIC, 
        REJPTIONFORMULA, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_BILLING_APPROVALLEVEL
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPROPTIONSPECIFIC, 
        APPROVALLEVELKEY, 
        APPROVALLEVELNAME, 
        APPROVALOPTION, 
        APPROVALOPTIONFORMULA, 
        APPROVALSCHEME, 
        APPROVEUPTOAMOUNT, 
        BYPASSAMOUNT, 
        COSIGN, 
        DELETED, 
        DESCRIPTION, 
        MODBY, 
        MODDTTM, 
        REJECTIONOPTION, 
        REJOPTIONSPECIFIC, 
        REJPTIONFORMULA, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_BILLING_APPROVALLEVEL;