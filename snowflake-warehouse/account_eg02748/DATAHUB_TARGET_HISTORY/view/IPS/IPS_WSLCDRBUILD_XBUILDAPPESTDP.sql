CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCDRBUILD_XBUILDAPPESTDP AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APBLDGAPPLDTLKEY, 
        APPESTTOTAL, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        PREQUOTETOTAL, 
        QUOTECOMPLETED, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XBUILDAPPESTDPKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCDRBUILD_XBUILDAPPESTDP
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APBLDGAPPLDTLKEY, 
        APPESTTOTAL, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        PREQUOTETOTAL, 
        QUOTECOMPLETED, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XBUILDAPPESTDPKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCDRBUILD_XBUILDAPPESTDP;