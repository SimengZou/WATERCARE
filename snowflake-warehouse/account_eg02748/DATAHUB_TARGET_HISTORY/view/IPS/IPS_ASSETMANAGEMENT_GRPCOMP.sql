CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_ASSETMANAGEMENT_GRPCOMP AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMPKEY, 
        DATALAKE_DELETED, 
        DISTFROMFT, 
        DISTFROMM, 
        DISTTOFT, 
        DISTTOM, 
        GROUPITEMKEY, 
        GROUPKEY, 
        INDEXNO, 
        MODBY, 
        MODDTTM, 
        RWFEATUREKEY, 
        RWFEATURETYPE, 
        RWROWP, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_ASSETMANAGEMENT_GRPCOMP
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMPKEY, 
        DATALAKE_DELETED, 
        DISTFROMFT, 
        DISTFROMM, 
        DISTTOFT, 
        DISTTOM, 
        GROUPITEMKEY, 
        GROUPKEY, 
        INDEXNO, 
        MODBY, 
        MODDTTM, 
        RWFEATUREKEY, 
        RWFEATURETYPE, 
        RWROWP, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_ASSETMANAGEMENT_GRPCOMP;