CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_RESOURCES_ESCROWTRN AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADJREAS, 
        DELETED, 
        ESCROWACCTKEY, 
        ESCROWPAYKEY, 
        MODBY, 
        MODDTTM, 
        TRNAMT, 
        TRNBY, 
        TRNDTTM, 
        TRNNO, 
        TRNTYPE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XFERREFTRNNO, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_RESOURCES_ESCROWTRN
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADJREAS, 
        DELETED, 
        ESCROWACCTKEY, 
        ESCROWPAYKEY, 
        MODBY, 
        MODDTTM, 
        TRNAMT, 
        TRNBY, 
        TRNDTTM, 
        TRNNO, 
        TRNTYPE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XFERREFTRNNO, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_RESOURCES_ESCROWTRN;