CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_BUILDING_BLDGCONSTRAINTS AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APTYPEKEY, 
        BLDGCONSTRAINTSKEY, 
        CONSTRAINTCONTORLTYPE, 
        CONSTRAINTNAME, 
        DATALAKE_DELETED, 
        MILESTONE, 
        MODBY, 
        MODDTTM, 
        SETTING, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_BUILDING_BLDGCONSTRAINTS
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APTYPEKEY, 
        BLDGCONSTRAINTSKEY, 
        CONSTRAINTCONTORLTYPE, 
        CONSTRAINTNAME, 
        DATALAKE_DELETED, 
        MILESTONE, 
        MODBY, 
        MODDTTM, 
        SETTING, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_BUILDING_BLDGCONSTRAINTS;