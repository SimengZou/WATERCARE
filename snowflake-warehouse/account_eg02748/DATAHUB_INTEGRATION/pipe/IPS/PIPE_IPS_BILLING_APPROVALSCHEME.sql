create or replace pipe DATAHUB_INTEGRATION.PIPE_IPS_BILLING_APPROVALSCHEME auto_ingest=true
            error_integration = ${buildvar.env}_NOTIFICATION_INTEGRATION_PIPE as
            copy into DATAHUB_LANDING.IPS_BILLING_APPROVALSCHEME
             from (
                SELECT
                $1,CURRENT_TIMESTAMP as ETL_LOAD_DATETIME,METADATA$FILENAME as ETL_LOAD_METADATAFILENAME
                FROM @DATAHUB_INTEGRATION.STAGE_IPS_BILLING_APPROVALSCHEME(FILE_FORMAT => DATAHUB_INTEGRATION.json_format));