create or replace TABLE FACT_METER_READING (
	METER_READING_SID NUMBER(38,0) NOT NULL autoincrement,
	WATER_METER_SID NUMBER(38,0) NOT NULL,
	READ_DATE_SID NUMBER(38,0) NOT NULL,
	READING_ADDED_SID NUMBER(38,0) NOT NULL,
	READ_SOURCE_SID NUMBER(38,0) NOT NULL,
	CUSTOMER_PROBLEM_SID NUMBER(38,0) NOT NULL,
	READER_CODE_SID NUMBER(38,0) NOT NULL,
	EMPLOYEE_SID NUMBER(38,0) NOT NULL,
	READ_REASON_SID NUMBER(38,0) NOT NULL,
	FIELD_NOTES VARCHAR(300),
	READING_KEY NUMBER(38,0),
	BILLABLE_USAGE FLOAT,
	READING FLOAT,
	USAGE FLOAT,
	METER_READING_ESTIMATE_FLAG VARCHAR(1),
	READY_FLAG VARCHAR(1),
	FINAL_FLAG VARCHAR(1),
	NO_READ_FLAG VARCHAR(1),
	WORK_ORDER NUMBER(38,0),
	INSPECTION NUMBER(38,0),
	ADDED_BY VARCHAR(30),
	LAST_MODIFIED_BY VARCHAR(30),
	READING_ADDED_DATE TIMESTAMP_NTZ(9),
	READ_DATE TIMESTAMP_NTZ(9),
	ETL_SOURCE_DATETIME TIMESTAMP_NTZ(9),
	ETL_RECORD_CREATED TIMESTAMP_NTZ(9),
	ETL_RECORD_UPDATED TIMESTAMP_NTZ(9),
	constraint UNIQUE_READING_KEY unique (READING_KEY)
);