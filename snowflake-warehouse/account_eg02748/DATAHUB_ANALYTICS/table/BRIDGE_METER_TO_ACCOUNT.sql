create or replace TABLE BRIDGE_METER_TO_ACCOUNT (
	BRIDGE_METER_TO_ACCOUNT_SID NUMBER(38,0) NOT NULL autoincrement,
	WATER_METER_SID NUMBER(38,0) NOT NULL,
	ACCOUNT_SID NUMBER(38,0) NOT NULL,
	ACCOUNT_SERVICE_SID NUMBER(38,0) NOT NULL,
	ACCOUNT_SERVICE_POSITION_KEY NUMBER(38,0),
	RECORD_END_DATE TIMESTAMP_NTZ(9),
	WATER_METER_KEY NUMBER(38,0),
	ACCOUNT_KEY NUMBER(38,0),
	ACCOUNT_SERVICE_KEY NUMBER(38,0),
	SUBTRACTIVE_FLAG VARCHAR(1),
	CONSUMPTION_PERCENTAGE FLOAT,
	CONSUMPTION_PERCENTAGE_FLAG VARCHAR(1),
	ETL_SOURCE_DATETIME TIMESTAMP_NTZ(9),
	ETL_RECORD_CREATED TIMESTAMP_NTZ(9),
	ETL_RECORD_UPDATED TIMESTAMP_NTZ(9),
	ETL_RECORD_DELETED TIMESTAMP_NTZ(9),
	CURRENT_RECORD VARCHAR(1) NOT NULL
);