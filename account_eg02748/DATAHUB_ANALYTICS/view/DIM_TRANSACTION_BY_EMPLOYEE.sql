create or replace view DIM_TRANSACTION_BY_EMPLOYEE(
	TRANSACTION_BY_EMPLOYEE_SID,
	EMPLOYEE_CODE,
	EMPLOYEE_CONTACT_KEY,
	EMPLOYEE_NAME,
	ETL_SOURCE_DATETIME,
	ETL_RECORD_CREATED,
	ETL_RECORD_UPDATED
) as
SELECT EMPLOYEE_SID AS TRANSACTION_BY_EMPLOYEE_SID , EMPLOYEE_CODE, EMPLOYEE_CONTACT_KEY, EMPLOYEE_NAME, ETL_SOURCE_DATETIME, ETL_RECORD_CREATED, ETL_RECORD_UPDATED
FROM DATAHUB_ANALYTICS.DIM_EMPLOYEE;