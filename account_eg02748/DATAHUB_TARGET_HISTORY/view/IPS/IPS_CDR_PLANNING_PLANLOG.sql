CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_PLANNING_PLANLOG AS 
    SELECT 
        ADDBY, 
        ADDBYPORTALUSR, 
        ADDDTTM, 
        APPLANINSPKEY, 
        APPLANKEY, 
        APPLANLOGKEY, 
        APPLANREVIEWKEY, 
        DATALAKE_DELETED, 
        LOGTYPE, 
        MODBY, 
        MODBYPORTALUSR, 
        MODDTTM, 
        STARTBY, 
        STARTDTTM, 
        STOPBY, 
        STOPDTTM, 
        TOTALTIME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_PLANNING_PLANLOG
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDBYPORTALUSR, 
        ADDDTTM, 
        APPLANINSPKEY, 
        APPLANKEY, 
        APPLANLOGKEY, 
        APPLANREVIEWKEY, 
        DATALAKE_DELETED, 
        LOGTYPE, 
        MODBY, 
        MODBYPORTALUSR, 
        MODDTTM, 
        STARTBY, 
        STARTDTTM, 
        STOPBY, 
        STOPDTTM, 
        TOTALTIME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_PLANNING_PLANLOG;