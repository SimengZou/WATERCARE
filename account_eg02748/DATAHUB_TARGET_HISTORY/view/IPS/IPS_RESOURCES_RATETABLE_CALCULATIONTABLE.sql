CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_RESOURCES_RATETABLE_CALCULATIONTABLE AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        CALCULATIONTABLEKEY, 
        CYCLEDAYS, 
        DELETED, 
        FORMULAINPUT, 
        FORMULALAST, 
        FORMULANAME, 
        FORMULAOUTPUT, 
        FORMULAUSED, 
        MAXIMUMCHARGE, 
        MAXIMUMUSED, 
        MINIMUMCHARGE, 
        MINIMUMUSED, 
        MODBY, 
        MODDTTM, 
        PRORATED, 
        RATETABLESETUPKEY, 
        USAGEDAYS, 
        USEFORPRORATING, 
        USEOVERAGE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_RESOURCES_RATETABLE_CALCULATIONTABLE
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        CALCULATIONTABLEKEY, 
        CYCLEDAYS, 
        DELETED, 
        FORMULAINPUT, 
        FORMULALAST, 
        FORMULANAME, 
        FORMULAOUTPUT, 
        FORMULAUSED, 
        MAXIMUMCHARGE, 
        MAXIMUMUSED, 
        MINIMUMCHARGE, 
        MINIMUMUSED, 
        MODBY, 
        MODDTTM, 
        PRORATED, 
        RATETABLESETUPKEY, 
        USAGEDAYS, 
        USEFORPRORATING, 
        USEOVERAGE, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_RESOURCES_RATETABLE_CALCULATIONTABLE;