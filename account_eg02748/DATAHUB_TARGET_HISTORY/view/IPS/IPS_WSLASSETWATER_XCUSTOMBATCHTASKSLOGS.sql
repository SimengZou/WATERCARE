CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLASSETWATER_XCUSTOMBATCHTASKSLOGS AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMMENTS, 
        DATALAKE_DELETED, 
        ENTITYKEY, 
        ENTITYNAME, 
        MODBY, 
        MODDTTM, 
        TASKNAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XCUSTOMBATCHTASKSLOGSKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLASSETWATER_XCUSTOMBATCHTASKSLOGS
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMMENTS, 
        DATALAKE_DELETED, 
        ENTITYKEY, 
        ENTITYNAME, 
        MODBY, 
        MODDTTM, 
        TASKNAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XCUSTOMBATCHTASKSLOGSKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLASSETWATER_XCUSTOMBATCHTASKSLOGS;