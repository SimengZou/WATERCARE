CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCDRBUILD_FEECODESMASTER AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        BUDGETNUMBER, 
        CODE, 
        DELETED, 
        DESCRIPT, 
        EFFDATE, 
        EXPDATE, 
        MODBY, 
        MODDTTM, 
        RATE, 
        UOM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCDRBUILD_FEECODESMASTER
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        BUDGETNUMBER, 
        CODE, 
        DELETED, 
        DESCRIPT, 
        EFFDATE, 
        EXPDATE, 
        MODBY, 
        MODDTTM, 
        RATE, 
        UOM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCDRBUILD_FEECODESMASTER;