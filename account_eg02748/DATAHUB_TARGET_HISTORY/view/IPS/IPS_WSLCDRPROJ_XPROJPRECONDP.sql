CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCDRPROJ_XPROJPRECONDP AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPROJAPPLDTLKEY, 
        BILLPAYER, 
        CONSTRUCTIONSTAGE, 
        CT, 
        DATALAKE_DELETED, 
        DP, 
        EPANUMBER, 
        LOT, 
        MODBY, 
        MODDTTM, 
        NEIGHBOURHOOD, 
        STAGE, 
        SUPERLOT, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XPROJPRECONDPKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCDRPROJ_XPROJPRECONDP
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPROJAPPLDTLKEY, 
        BILLPAYER, 
        CONSTRUCTIONSTAGE, 
        CT, 
        DATALAKE_DELETED, 
        DP, 
        EPANUMBER, 
        LOT, 
        MODBY, 
        MODDTTM, 
        NEIGHBOURHOOD, 
        STAGE, 
        SUPERLOT, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XPROJPRECONDPKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCDRPROJ_XPROJPRECONDP;