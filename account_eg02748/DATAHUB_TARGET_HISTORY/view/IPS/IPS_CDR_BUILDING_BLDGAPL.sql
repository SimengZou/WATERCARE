CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_BUILDING_BLDGAPL AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APBLDGKEY, 
        APLTY, 
        BLDGAPLKEY, 
        CAPACITY, 
        CNTCTKEY, 
        DATALAKE_DELETED, 
        EFFDATE, 
        EXPDATE, 
        MODBY, 
        MODDTTM, 
        PRIM, 
        RESPACCOUNTHOLDERFLAG, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_BUILDING_BLDGAPL
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APBLDGKEY, 
        APLTY, 
        BLDGAPLKEY, 
        CAPACITY, 
        CNTCTKEY, 
        DATALAKE_DELETED, 
        EFFDATE, 
        EXPDATE, 
        MODBY, 
        MODDTTM, 
        PRIM, 
        RESPACCOUNTHOLDERFLAG, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_BUILDING_BLDGAPL;