CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_METERMANAGEMENT_WATER_RTADDR AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRKEY, 
        COMMENTS, 
        DATALAKE_DELETED, 
        FIELDCODE, 
        INDEXNO, 
        MANUALREVIEWFLAG, 
        MODBY, 
        MODDTTM, 
        POSITION, 
        REREADSRVREQNO, 
        ROUTEKEY, 
        RTADDRKEY, 
        SEQNO, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_METERMANAGEMENT_WATER_RTADDR
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRKEY, 
        COMMENTS, 
        DATALAKE_DELETED, 
        FIELDCODE, 
        INDEXNO, 
        MANUALREVIEWFLAG, 
        MODBY, 
        MODDTTM, 
        POSITION, 
        REREADSRVREQNO, 
        ROUTEKEY, 
        RTADDRKEY, 
        SEQNO, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_METERMANAGEMENT_WATER_RTADDR;