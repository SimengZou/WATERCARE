CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCDRPLAN_XPLANDPPROPINTAKE AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        DATEOFAPPLICATION, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        PLANAPPLDETAILKEY, 
        PROPOSALNAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XPLANDPPROPINTAKEKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCDRPLAN_XPLANDPPROPINTAKE
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        DATEOFAPPLICATION, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        PLANAPPLDETAILKEY, 
        PROPOSALNAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XPLANDPPROPINTAKEKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCDRPLAN_XPLANDPPROPINTAKE;