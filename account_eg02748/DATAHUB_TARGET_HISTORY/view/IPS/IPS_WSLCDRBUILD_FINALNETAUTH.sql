CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCDRBUILD_FINALNETAUTH AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APBLDGAPPLDTLKEY, 
        ASBUILTS, 
        CS3, 
        DATALAKE_DELETED, 
        FINALNETAUTHKEY, 
        MODBY, 
        MODDTTM, 
        PHOTOS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCDRBUILD_FINALNETAUTH
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APBLDGAPPLDTLKEY, 
        ASBUILTS, 
        CS3, 
        DATALAKE_DELETED, 
        FINALNETAUTHKEY, 
        MODBY, 
        MODDTTM, 
        PHOTOS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCDRBUILD_FINALNETAUTH;