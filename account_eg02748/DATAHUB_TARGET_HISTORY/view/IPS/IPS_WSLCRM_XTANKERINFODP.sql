CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCRM_XTANKERINFODP AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        BONDPAID, 
        BONDREFUNDED, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        MOHEXPIRY, 
        SERVICESTARTDATE, 
        SERVNO, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XTANKERINFODPKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCRM_XTANKERINFODP
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        BONDPAID, 
        BONDREFUNDED, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        MOHEXPIRY, 
        SERVICESTARTDATE, 
        SERVNO, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XTANKERINFODPKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCRM_XTANKERINFODP;