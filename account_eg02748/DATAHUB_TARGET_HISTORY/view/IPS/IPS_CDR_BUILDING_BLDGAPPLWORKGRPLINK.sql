CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_BUILDING_BLDGAPPLWORKGRPLINK AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLWORKGROUPLINKKEY, 
        APPLWORKGRP, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        WORKOCCFILTERGRP, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_BUILDING_BLDGAPPLWORKGRPLINK
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLWORKGROUPLINKKEY, 
        APPLWORKGRP, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        WORKOCCFILTERGRP, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_BUILDING_BLDGAPPLWORKGRPLINK;