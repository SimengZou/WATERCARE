CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_COMMONPLANCOPY AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMMONPLANCOPYKEY, 
        CONTACTKEY, 
        COPYID, 
        COPYNO, 
        COPYSTATUS, 
        COPYSTATUSDTTM, 
        CURRENTLOC, 
        CURRENTLOCDTTM, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        OUTSOURCE, 
        PLANCOPYTYPE, 
        REVIEWER, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VERSION, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_COMMONPLANCOPY
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        COMMONPLANCOPYKEY, 
        CONTACTKEY, 
        COPYID, 
        COPYNO, 
        COPYSTATUS, 
        COPYSTATUSDTTM, 
        CURRENTLOC, 
        CURRENTLOCDTTM, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        OUTSOURCE, 
        PLANCOPYTYPE, 
        REVIEWER, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VERSION, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_COMMONPLANCOPY;