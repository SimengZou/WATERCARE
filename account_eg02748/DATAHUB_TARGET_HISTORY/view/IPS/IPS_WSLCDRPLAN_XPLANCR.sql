CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLCDRPLAN_XPLANCR AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLANKEY, 
        APPLICAITONALERT, 
        DATEOFAPPLICATION, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        PROPOSALNAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XPLANCRKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLCDRPLAN_XPLANCR
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPLANKEY, 
        APPLICAITONALERT, 
        DATEOFAPPLICATION, 
        DELETED, 
        MODBY, 
        MODDTTM, 
        PROPOSALNAME, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XPLANCRKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLCDRPLAN_XPLANCR;