CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_BILLING_ACCTPAGEQLINKLAYOUT AS 
    SELECT 
        ACCTPAGEQLINKLAYOUTKEY, 
        ACCTPAGEQLINKSUKEY, 
        ADDBY, 
        ADDDTTM, 
        DATALAKE_DELETED, 
        DISPLAYFLAG, 
        DISPLAYORDER, 
        MODBY, 
        MODDTTM, 
        QUICKLINK, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_BILLING_ACCTPAGEQLINKLAYOUT
    UNION ALL 
    SELECT 
        ACCTPAGEQLINKLAYOUTKEY, 
        ACCTPAGEQLINKSUKEY, 
        ADDBY, 
        ADDDTTM, 
        DATALAKE_DELETED, 
        DISPLAYFLAG, 
        DISPLAYORDER, 
        MODBY, 
        MODDTTM, 
        QUICKLINK, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_BILLING_ACCTPAGEQLINKLAYOUT;