CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_CDR_PROJECT_PROJFAILED AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPROJFAILKEY, 
        APPROJINSPKEY, 
        CMPLDTTM, 
        COMMENTS, 
        DATALAKE_DELETED, 
        FAILED, 
        LOC, 
        MODBY, 
        MODDTTM, 
        STATUS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VIOLDTTM, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_CDR_PROJECT_PROJFAILED
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        APPROJFAILKEY, 
        APPROJINSPKEY, 
        CMPLDTTM, 
        COMMENTS, 
        DATALAKE_DELETED, 
        FAILED, 
        LOC, 
        MODBY, 
        MODDTTM, 
        STATUS, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        VIOLDTTM, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_CDR_PROJECT_PROJFAILED;