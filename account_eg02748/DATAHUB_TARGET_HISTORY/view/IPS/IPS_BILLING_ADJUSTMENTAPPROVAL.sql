CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_BILLING_ADJUSTMENTAPPROVAL AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADJUSTMENT, 
        ADJUSTMENTAPPROVALKEY, 
        APPROVALCOMMENTS, 
        APPROVALDTTM, 
        APPROVALLEVELKEY, 
        APPROVEDBY, 
        DELETED, 
        DISPLAYEDFLAG, 
        MODBY, 
        MODDTTM, 
        REJECTEDBY, 
        REJECTIONCOMMENTS, 
        REJECTIONDTTM, 
        STATUS, 
        SUBMISSIONCOMMENTS, 
        SUBMISSIONDTTM, 
        SUBMITTEDBY, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_BILLING_ADJUSTMENTAPPROVAL
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADJUSTMENT, 
        ADJUSTMENTAPPROVALKEY, 
        APPROVALCOMMENTS, 
        APPROVALDTTM, 
        APPROVALLEVELKEY, 
        APPROVEDBY, 
        DELETED, 
        DISPLAYEDFLAG, 
        MODBY, 
        MODDTTM, 
        REJECTEDBY, 
        REJECTIONCOMMENTS, 
        REJECTIONDTTM, 
        STATUS, 
        SUBMISSIONCOMMENTS, 
        SUBMISSIONDTTM, 
        SUBMITTEDBY, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_BILLING_ADJUSTMENTAPPROVAL;