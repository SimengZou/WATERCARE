CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_ASSETMANAGEMENT_COMPADDR AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRKEY, 
        ADDRQUAL, 
        COMPADDKEY, 
        COMPKEY, 
        COMPTYPE, 
        DATALAKE_DELETED, 
        DELFLAG, 
        DISTFROMFT, 
        DISTFROMM, 
        DISTTOFT, 
        DISTTOM, 
        MODBY, 
        MODDTTM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_ASSETMANAGEMENT_COMPADDR
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRKEY, 
        ADDRQUAL, 
        COMPADDKEY, 
        COMPKEY, 
        COMPTYPE, 
        DATALAKE_DELETED, 
        DELFLAG, 
        DISTFROMFT, 
        DISTFROMM, 
        DISTTOFT, 
        DISTTOM, 
        MODBY, 
        MODDTTM, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_ASSETMANAGEMENT_COMPADDR;