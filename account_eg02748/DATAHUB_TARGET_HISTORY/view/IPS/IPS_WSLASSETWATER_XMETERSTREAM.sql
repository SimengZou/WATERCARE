CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLASSETWATER_XMETERSTREAM AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRESS, 
        COMPKEY, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        OTHERMETER, 
        RELATIONSHIP, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XMETERSTREAMKEY, 
        XWATERMETERDETAILKEY, 
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLASSETWATER_XMETERSTREAM
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRESS, 
        COMPKEY, 
        DATALAKE_DELETED, 
        MODBY, 
        MODDTTM, 
        OTHERMETER, 
        RELATIONSHIP, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        XMETERSTREAMKEY, 
        XWATERMETERDETAILKEY, 
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLASSETWATER_XMETERSTREAM;