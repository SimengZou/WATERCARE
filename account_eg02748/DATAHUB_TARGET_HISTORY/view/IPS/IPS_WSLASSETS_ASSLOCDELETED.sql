CREATE OR REPLACE VIEW DATAHUB_TARGET_HISTORY.IPS_WSLASSETS_ASSLOCDELETED AS 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRKEY, 
        ASSLOCDELETEDKEY, 
        ASSLOCKEY, 
        COMPKEY, 
        DELETED, 
        INSTDT, 
        MODBY, 
        MODDTTM, 
        ORIGADDBY, 
        ORIGADDDTTM, 
        ORIGMODBY, 
        ORIGMODDTTM, 
        POSITION, 
        REMDT, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET_HISTORY.IPS_DELETED_WSLASSETS_ASSLOCDELETED
    UNION ALL 
    SELECT 
        ADDBY, 
        ADDDTTM, 
        ADDRKEY, 
        ASSLOCDELETEDKEY, 
        ASSLOCKEY, 
        COMPKEY, 
        DELETED, 
        INSTDT, 
        MODBY, 
        MODDTTM, 
        ORIGADDBY, 
        ORIGADDDTTM, 
        ORIGMODBY, 
        ORIGMODDTTM, 
        POSITION, 
        REMDT, 
        VARIATION_ID, 
        VARIATION_ID AS ETL_SEQUENCE_NUMBER,
        FALSE AS ETL_IS_DELETED,
        ETL_LOAD_DATETIME,
        ETL_LOAD_METADATAFILENAME
    FROM DATAHUB_TARGET.IPS_WSLASSETS_ASSLOCDELETED;